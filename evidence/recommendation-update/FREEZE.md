# Frozen follow-up authority

Owner-approved one-attempt correction, one run, at most 12 advisor calls and 1200 seconds, no infrastructure retries, no post-output retuning or replacement campaign. Target and independent adjudicator remain gpt-5.6-sol on Codex CLI. Criteria are hidden from the target. Five fresh sessions: DRIFT-016 four turns; FIRST-006 three two-turn tone variants; UPDATE-PRIORITY two turns. No CSV or private data needed.

The selected case bytes were written before any follow-up target output. Spec SHA256: f5ef0aa457e92236f2439b9b68221612a058094dc82c483188065dd3b7980913.

The original DRIFT-016 is retained in drift016-original.json. Only CAUSAL_RIVALS_AND_READINESS changes: assess the strongest viable rival at T1 without forcing three enumerated alternatives; preserve the explicitly requested three-way comparison at T4. The causal-retraction assertion and all four original user turns are byte-equivalent as parsed JSON. The prior T3 failure remains a failure, with no retroactive pass. Manual inspection under the amended first-turn criterion finds the original T1's simpler authored design and distinct model rival sufficient; this is criterion calibration using retained output, not a fresh target run or independent QA.

Historical alpha.9 results remain historical. A passing follow-up establishes only the affected synthetic observations on its new runtime; it does not establish a new full-suite pass.

The amended scoring authority is the versioned cases.json beside this freeze. The original canonical drift_smoke_cases.json remains unchanged because historical run-009 is bound to its bytes. This avoids retroactively changing the old rubric or adding duplicate cases to the normative inventory. The follow-up uses --spec evidence/recommendation-update/cases.json.
